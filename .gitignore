# Dependency directories
node_modules/
client_secret_244391185334-ajae4isl9h8903l3ul3ph83ttqllkmms.apps.googleusercontent.com.json
.yarn/
# Build directories
build/
# Stale lockfile from a previous yarn era — pnpm is the package manager.
yarn.lock

# Next.js build output
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE and editor directories
.idea/
.vscode/
*.swp
*.swo

# OS generated files
.DS_Store
Thumbs.db

# TypeScript type checking
*.tsbuildinfo

# TailwindCSS JIT generated styles
tailwind.css
safelist.txt

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Miscellaneous
.cache/
dist/
*.gz

# One-off script outputs (creds CSVs, review CSVs, etc. — never commit)
tmp/

# Design assets (not part of the shipped app)
HajjScanner Design System/
HajjScanner Design System.zip

# ---------------------------------------------------------------------------
# Sensitive data / internal docs — anything below this line MUST stay out of
# the repo. If a file matching one of these patterns is already tracked,
# `git rm --cached <file>` it (and then scrub history with git-filter-repo
# if it ever contained real credentials).
# ---------------------------------------------------------------------------
# Spreadsheets, DB dumps, internal docs
*.xlsx
*.csv
*.docx
*.pdf
# Bundled source archives — never check these in
*.zip
*.tar
*.tar.gz
*.7z
# Internal screenshots / design exports not used by the app
tabs.png
# Empty placeholder that has no business being in the repo
sitemap_live.xml
# Legacy script registry from the Chisfis template
package-scripts.json
